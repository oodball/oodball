{
  "name": "portfolio-website",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://oodball.com",
  "dependencies": {
    "@supabase/supabase-js": "^2.57.4",
    "cors": "^2.8.5",
    "date-fns": "^4.1.0",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "heic-convert": "^2.1.0",
    "heic2any": "^0.0.4",
    "numpy": "^0.0.1",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-markdown": "^10.1.0",
    "react-router-dom": "^7.7.0",
    "react-scripts": "5.0.1",
    "rehype-raw": "^7.0.0",
    "sharp": "^0.34.3",
    "sklearn": "^1.0.0",
    "web-push": "^3.6.7"
  },
  "scripts": {
    "prebuild": "npm run generate-manifest",
    "start": "npm run generate-manifest && react-scripts start",
    "build": "react-scripts build",
    "build:clean": "npm run generate-manifest && rm -rf build && react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "generate-manifest": "node scripts/generate-manifest.js",
    "watch-manifest": "node scripts/generate-manifest.js watch"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "chokidar": "^5.0.0"
  }
}
